# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Ensure privacy is met with API URLs and keys

Look for lines of code that references the .env file using "process.env" and prompt for user to create .env file, and put there the different API URLs and keys used accross the project.

# Ensure metadata is preserved

Do not delete LICENSE
